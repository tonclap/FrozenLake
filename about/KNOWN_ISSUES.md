# Known Issues — FrozenLake

Формат: **описание** — условие воспроизведения — обходной путь / статус.

- **Мусорный `BIT551B.tmp` ~2.8 ГБ в корне** — остался от прерванной операции
  (дата файла 2026-01-19) — удалить вручную; добавлен в `.gitignore`, в историю
  не попадает.
- **Нет `requirements.txt` с пинами** — воспроизводимость окружения не
  гарантирована — ставить `gym numpy torch tensorboard` и пинить версии локально.
- **Проект архивный** — активная разработка не ведётся.
